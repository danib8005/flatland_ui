[project]
name = "flatland-dispatcher-backend"
version = "0.1.0"
description = "Flatland Human-in-the-Loop Dispatcher Backend"
requires-python = ">=3.12"

[tool.ruff]
line-length = 120
target-version = "py312"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
